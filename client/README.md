
  # Production Ready Application

  This is a code bundle for Production Ready Application. The original project is available at https://www.figma.com/design/R3F80CVtK3WCMWuUr9IRBc/Production-Ready-Application.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  